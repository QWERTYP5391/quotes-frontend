import * as React from "react";

class Header extends React.Component {

    render() {
        return <div className="header">Social Media platform where a user can read and write quotes. Quotes are
            retrieved from the cloud</div>
    }

}

export default Header;